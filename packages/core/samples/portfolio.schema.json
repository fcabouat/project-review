{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "portfolio.schema.json",
  "title": "Portfolio — project review",
  "description": "Closed v4 portfolio shape. The application additionally checks calendar dates, per-collection id uniqueness, ascending font-weight ranges, unique font family/weight/style slots after defaults, summed font data-URI size (2 000 000 characters), total categories/projects/free slides (2000), and normalized document size (10 000 000 characters). Nested collections are each limited to 2000 items. These semantic and total-size checks remain the strict parser's responsibility.",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "review", "settings", "categories", "projects", "freeSlides"],
  "definitions": {
    "paletteColor": {
      "type": "string",
      "pattern": "^#[0-9a-fA-F]{6}$",
      "description": "Exact six-digit hexadecimal — the shape the parse and the editor's commands both enforce (core values/palette.ts)"
    },
    "isoDate": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
    },
    "id": {
      "type": "string",
      "minLength": 1,
      "pattern": "^(?:[^\\uD800-\\uDFFF]|[\\uD800-\\uDBFF][\\uDC00-\\uDFFF])*$",
      "description": "Non-empty well-formed Unicode; unpaired surrogates are refused, never replaced or normalized."
    },
    "color": {
      "enum": [
        "blue",
        "indigo",
        "teal",
        "cyan",
        "green",
        "olive",
        "amber",
        "orange",
        "red",
        "purple",
        "brown",
        "taupe"
      ]
    },
    "stage": {
      "enum": ["toScope", "ready", "inProgress", "residuals", "closed", "abandoned"]
    },
    "healthLevel": {
      "enum": ["onTrack", "watch", "alert", "critical"]
    },
    "priority": {
      "enum": ["P1", "P2", "P3"]
    },
    "decision": {
      "type": "object",
      "additionalProperties": false,
      "required": ["question"],
      "properties": {
        "question": {
          "type": "string",
          "description": "A closed question the review session can settle"
        },
        "decider": {
          "type": "string"
        },
        "taken": {
          "type": "object",
          "additionalProperties": false,
          "required": ["text", "when"],
          "properties": {
            "text": {
              "type": "string"
            },
            "when": {
              "$ref": "#/definitions/isoDate"
            }
          }
        }
      }
    },
    "milestone": {
      "type": "object",
      "additionalProperties": false,
      "required": ["label", "date", "done"],
      "properties": {
        "label": {
          "type": "string"
        },
        "date": {
          "$ref": "#/definitions/isoDate"
        },
        "display": {
          "type": "string"
        },
        "done": {
          "type": "boolean"
        }
      }
    }
  },
  "properties": {
    "version": {
      "const": 4
    },
    "review": {
      "type": "object",
      "additionalProperties": false,
      "required": ["title", "reviewDate"],
      "properties": {
        "title": {
          "type": "string"
        },
        "subtitle": {
          "type": "string"
        },
        "reviewDate": {
          "$ref": "#/definitions/isoDate"
        },
        "previousReviewDate": {
          "$ref": "#/definitions/isoDate"
        }
      }
    },
    "settings": {
      "type": "object",
      "additionalProperties": false,
      "required": ["identity", "show", "recapRows"],
      "properties": {
        "language": {
          "enum": ["fr", "en"],
          "default": "fr"
        },
        "navigation": {
          "enum": ["sections", "linear"],
          "description": "Absent means sections (horizontal groups with vertical slides); linear puts every slide horizontally."
        },
        "identity": {
          "type": "object",
          "additionalProperties": false,
          "required": ["org", "unit"],
          "properties": {
            "org": {
              "type": "string"
            },
            "unit": {
              "type": "string"
            },
            "orgLong": {
              "type": "string"
            },
            "unitLong": {
              "type": "string"
            },
            "contact": {
              "type": "string"
            },
            "logo": {
              "type": "string",
              "pattern": "^data:image\\/",
              "maxLength": 400000,
              "description": "Logo inlined as a data URI (SVG/PNG/JPEG/WebP); absent → no logo is drawn"
            }
          }
        },
        "theme": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "style": {
              "enum": ["flat", "institutional", "modern"],
              "default": "flat"
            },
            "palette": {
              "enum": ["material", "tailwind", "uniform"],
              "default": "material"
            },
            "font": {
              "type": "string",
              "pattern": "^[A-Za-z0-9 _-]{1,64}$",
              "default": "Roboto"
            },
            "fontFaces": {
              "type": "array",
              "uniqueItems": true,
              "description": "Embedded woff2 faces. Each family/weight/style slot must be unique, after the optional weight/style defaults; the parse enforces slot uniqueness even when dataUri differs (uniqueItems only rejects identical objects). All faces together must stay under 2 000 000 data-URI characters; only the parse counts that total.",
              "items": {
                "type": "object",
                "additionalProperties": false,
                "required": ["family", "dataUri"],
                "properties": {
                  "family": {
                    "type": "string",
                    "pattern": "^[A-Za-z0-9 _-]{1,64}$"
                  },
                  "weight": {
                    "type": "string",
                    "pattern": "^(?:[1-8][0-9]{2}|900)(?: (?:[1-8][0-9]{2}|900))?$",
                    "default": "400",
                    "description": "One integer in 100–900, or an ascending \"min max\" pair. The RANGE is stated by the pattern; only the parse can state that a pair ascends."
                  },
                  "style": {
                    "enum": ["normal", "italic"],
                    "default": "normal"
                  },
                  "dataUri": {
                    "type": "string",
                    "pattern": "^data:font\\/woff2;base64,[A-Za-z0-9+/]+={0,2}$",
                    "maxLength": 550000
                  }
                }
              }
            },
            "customPalette": {
              "type": "object",
              "additionalProperties": false,
              "required": ["colors"],
              "description": "The twelve category colours carried BY THE PORTFOLIO instead of chosen among the built-in families — it takes precedence over \"palette\" for as long as it is there. The table is exhaustive: the twelve names of the domain, no more and no less.",
              "properties": {
                "label": {
                  "type": "string",
                  "description": "What to call it in the interface; absent → a generic label"
                },
                "colors": {
                  "type": "object",
                  "additionalProperties": false,
                  "required": [
                    "blue",
                    "indigo",
                    "teal",
                    "cyan",
                    "green",
                    "olive",
                    "amber",
                    "orange",
                    "red",
                    "purple",
                    "brown",
                    "taupe"
                  ],
                  "properties": {
                    "blue": {
                      "$ref": "#/definitions/paletteColor"
                    },
                    "indigo": {
                      "$ref": "#/definitions/paletteColor"
                    },
                    "teal": {
                      "$ref": "#/definitions/paletteColor"
                    },
                    "cyan": {
                      "$ref": "#/definitions/paletteColor"
                    },
                    "green": {
                      "$ref": "#/definitions/paletteColor"
                    },
                    "olive": {
                      "$ref": "#/definitions/paletteColor"
                    },
                    "amber": {
                      "$ref": "#/definitions/paletteColor"
                    },
                    "orange": {
                      "$ref": "#/definitions/paletteColor"
                    },
                    "red": {
                      "$ref": "#/definitions/paletteColor"
                    },
                    "purple": {
                      "$ref": "#/definitions/paletteColor"
                    },
                    "brown": {
                      "$ref": "#/definitions/paletteColor"
                    },
                    "taupe": {
                      "$ref": "#/definitions/paletteColor"
                    }
                  }
                }
              }
            }
          }
        },
        "show": {
          "type": "object",
          "additionalProperties": false,
          "required": ["healthDashboard", "recap", "archives", "decisions"],
          "properties": {
            "healthDashboard": {
              "type": "boolean"
            },
            "recap": {
              "type": "boolean"
            },
            "archives": {
              "type": "boolean"
            },
            "decisions": {
              "type": "boolean"
            }
          }
        },
        "recapRows": {
          "type": "integer",
          "minimum": 6,
          "maximum": 16
        }
      }
    },
    "categories": {
      "type": "array",
      "maxItems": 2000,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "name", "color"],
        "properties": {
          "id": {
            "$ref": "#/definitions/id"
          },
          "name": {
            "type": "string"
          },
          "color": {
            "$ref": "#/definitions/color"
          }
        }
      }
    },
    "projects": {
      "type": "array",
      "maxItems": 2000,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "id",
          "name",
          "categoryId",
          "stage",
          "onHold",
          "goal",
          "done",
          "ongoing",
          "next",
          "decisions",
          "milestones",
          "sheet"
        ],
        "properties": {
          "id": {
            "$ref": "#/definitions/id"
          },
          "name": {
            "type": "string"
          },
          "categoryId": {
            "type": "string",
            "pattern": "^(?:[^\\uD800-\\uDFFF]|[\\uD800-\\uDBFF][\\uDC00-\\uDFFF])*$",
            "description": "A category id, or \"\" while the project is unassigned"
          },
          "priority": {
            "$ref": "#/definitions/priority"
          },
          "stage": {
            "$ref": "#/definitions/stage"
          },
          "onHold": {
            "type": "boolean"
          },
          "health": {
            "$ref": "#/definitions/healthLevel"
          },
          "progress": {
            "type": "integer",
            "minimum": 0,
            "maximum": 100
          },
          "lead": {
            "type": "string"
          },
          "sponsor": {
            "type": "string"
          },
          "scope": {
            "type": "string"
          },
          "goal": {
            "type": "string"
          },
          "budget": {
            "type": "string"
          },
          "start": {
            "$ref": "#/definitions/isoDate"
          },
          "targetEnd": {
            "$ref": "#/definitions/isoDate"
          },
          "actualEnd": {
            "$ref": "#/definitions/isoDate"
          },
          "done": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "maxItems": 2000
          },
          "ongoing": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "maxItems": 2000
          },
          "next": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "maxItems": 2000
          },
          "risks": {
            "type": "string"
          },
          "decisions": {
            "type": "array",
            "items": {
              "$ref": "#/definitions/decision"
            },
            "maxItems": 2000
          },
          "milestones": {
            "type": "array",
            "items": {
              "$ref": "#/definitions/milestone"
            },
            "maxItems": 2000
          },
          "sheet": {
            "enum": ["auto", "always", "never"]
          },
          "updatedOn": {
            "$ref": "#/definitions/isoDate"
          },
          "author": {
            "type": "string"
          },
          "reference": {
            "type": "string"
          }
        }
      }
    },
    "freeSlides": {
      "type": "array",
      "maxItems": 2000,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "anchor", "title", "blocks"],
        "properties": {
          "id": {
            "$ref": "#/definitions/id"
          },
          "anchor": {
            "oneOf": [
              {
                "type": "object",
                "additionalProperties": false,
                "required": ["type"],
                "properties": {
                  "type": {
                    "enum": ["opening", "closing"]
                  }
                }
              },
              {
                "type": "object",
                "additionalProperties": false,
                "required": ["type", "categoryId"],
                "properties": {
                  "type": {
                    "const": "beforeCategory"
                  },
                  "categoryId": {
                    "$ref": "#/definitions/id"
                  }
                }
              }
            ]
          },
          "title": {
            "type": "string"
          },
          "blocks": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "array",
              "items": {
                "type": "string",
                "description": "One bullet line — **bold** and a trailing \" — detail\" suffix only"
              },
              "maxItems": 2000
            },
            "maxItems": 2000
          }
        }
      }
    }
  }
}
