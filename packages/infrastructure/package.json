{
  "name": "@project-review/infrastructure",
  "private": true,
  "version": "0.2.0",
  "description": "Browser adapters behind the core's declared interfaces: localStorage + setTimeout scheduler, hash router, runtime font loader, standalone slideshow export. Depends only on @project-review/core.",
  "license": "MIT",
  "type": "module",
  "files": [
    "src"
  ],
  "exports": {
    "./*": "./src/*.ts"
  },
  "scripts": {
    "check": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@project-review/core": "workspace:*",
    "reveal.js": "^6.0.1"
  }
}
